# Python
__pycache__/
*.pyc
.venv/
venv/
python/
.uvcache/
.env
.DS_Store

# Runtime data (uploads/outputs) — never push
jobs/

# Local secret config — never push (admin password ahiya hoy chhe)
config.yaml

# Local helper scripts — GitHub ma push karva nathi
push_to_github.bat
run_local.bat
